import { FAQ } from "@/components/ui/faq-section";

const FAQComponent = () => {
  return (
    <section id="faq" className="bg-gradient-to-b from-background to-background/95">
      <FAQ />
    </section>
  );
};

export default FAQComponent;
